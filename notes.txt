React frontend
- editable instructions
- general rules
- genre-specific rules
- selected examples
- conversation history
- current approved document
- newest user message


React request

↓

Cloudflare reads the request body

↓

Cloudflare sends the context to OpenAI

↓

OpenAI generates the next response

↓

Cloudflare wraps it in our AssistantResponse shape

↓

React displays it





npm install -D @cloudflare/workers-types


Running together locally:
npm run build
npx wrangler pages dev dist
http://localhost:8788


Qs for the end:
- write a prompt for claude explaining what everything in the repo is 
- how would I switch this away from Cloudflare
- is there a way for copilot to keep the context so we don't have to send back and forth each time